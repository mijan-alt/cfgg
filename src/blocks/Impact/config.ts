import { Block } from "payload"

export const Impact: Block = {
  slug: 'impact',
  interfaceName:'impact',
  labels: {
    singular: 'Impact Section',
    plural: 'Impact Sections',
  },
  fields: [], // No fields, as data is hardcoded
}
